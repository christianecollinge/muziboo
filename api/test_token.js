import { createClient } from '@supabase/supabase-js';

const supabaseUrl = 'https://paydmwknrjmjlswyouem.supabase.co';
const supabaseKey = 'your-anon-key-here'; // We can't actually get a token without the real creds in code
// Actually I don't have the user's password, so I can't generate a token myself from CLI.
